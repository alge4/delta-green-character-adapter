# Green Agent Creator `5c9e92d` fixtures

Reviewed for the initial tracer-bullet release (#29).

## Provenance

| Artifact | Provenance | Licence / reuse |
|---|---|---|
| `caleb.json` | Owner-supplied golden export for Green Agent Creator commit `5c9e92d987f1251d62c172209fc53f8e8ac3372b`. SHA-256 recorded in `SHA256SUMS`. | Retained with owner consent for adapter contract tests only; not scraped from the community library. |
| `synthetic/*.json` | Deterministic synthetics authored in this repository from the pinned GAC source contract (`docs/research/green-agent-creator-5c9e92d-contract.md`). | MIT, same as this repository. |

Community-library payloads are intentionally absent. See `docs/research/green-agent-creator-sample-corpus.md`.

## Integrity

`SHA256SUMS` lists every JSON fixture in this directory. CI fails when advertised Green capability fixtures disagree with these digests.
