{
  "name": "delta-green-character-adapter",
  "version": "0.0.0",
  "private": true,
  "description": "Canonical Delta Green character model and adapters for Foundry VTT, spreadsheets, JSON, and PDF.",
  "license": "MIT",
  "packageManager": "pnpm@10.14.0",
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm -r check",
    "test": "pnpm -r test"
  },
  "engines": {
    "node": ">=22"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
