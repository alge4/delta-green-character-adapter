{
  "name": "@delta-green-character-adapter/adapter-core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./dist/index.js"
    }
  },
  "types": "./src/index.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm --filter @delta-green-character-adapter/character-model --filter @delta-green-character-adapter/validation build && tsc -p tsconfig.test.json && node --test .test-dist/test/**/*.test.js"
  },
  "dependencies": {
    "@delta-green-character-adapter/character-model": "workspace:*",
    "@delta-green-character-adapter/validation": "workspace:*",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/node": "^22.18.1",
    "typescript": "^5.9.2"
  }
}
