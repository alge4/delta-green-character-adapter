{
  "name": "@delta-green-character-adapter/adapter-foundry-deltagreen",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./dist/index.js"
    }
  },
  "types": "./src/index.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "fixtures": "node scripts/generate-synthetic-fixtures.mjs",
    "test": "pnpm --filter @delta-green-character-adapter/adapter-core --filter @delta-green-character-adapter/character-model build && tsc -p tsconfig.test.json && node --test .test-dist/test/**/*.test.js"
  },
  "dependencies": {
    "@delta-green-character-adapter/adapter-core": "workspace:*",
    "@delta-green-character-adapter/character-model": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.18.1",
    "typescript": "^5.9.2"
  }
}
